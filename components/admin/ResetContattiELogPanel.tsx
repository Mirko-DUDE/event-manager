'use client'

/**
 * Stub Passo 1 — placeholder per la "Zona pericolosa" (Passo 3).
 * Nessuna azione di reset qui: solo montaggio del campo ui sul Global.
 */
export default function ResetContattiELogPanel() {
  return (
    <div>
      <p>
        Zona pericolosa — UI di reset in arrivo (Passo 3). Le azioni di hard delete non sono
        ancora disponibili.
      </p>
    </div>
  )
}
